--- /usr/bin/dstat	2021-06-22 15:50:49.725636451 +0100
+++ /usr/bin/dstat	2021-06-22 15:56:07.810237312 +0100
@@ -544,12 +544,15 @@

         line = ''
         for i, name in enumerate(self.vars):
-            if isinstance(self.val[name], types.ListType) or isinstance(self.val[name], types.TupleType):
+            list_type = types.ListType if six.PY2 else list
+            tuple_type = types.TupleType if six.PY2 else tuple
+            string_type = types.StringType if six.PY2 else str
+            if isinstance(self.val[name], list_type) or isinstance(self.val[name], tuple_type):
                 for j, val in enumerate(self.val[name]):
                     line = line + printcsv(val)
                     if j + 1 != len(self.val[name]):
                         line = line + char['sep']
-            elif isinstance(self.val[name], types.StringType):
+            elif isinstance(self.val[name], string_type):
                 line = line + self.val[name]
             else:
                 line = line + printcsv(self.val[name])