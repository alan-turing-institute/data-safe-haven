---

version: 2
updates:
  # GitHub Action update PRs
  - package-ecosystem: "github-actions"
    directory: "/"  # This is recursive so it also checks .github/workflows/
    schedule:
      interval: "weekly"
