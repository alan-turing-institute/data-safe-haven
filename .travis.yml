---
os: linux
dist: focal

language: minimal

jobs:
  include:
    - name: Lint Ansible
      language: python
      python: 3.9
      before_script:
        - pip install "ansible-lint[core]"
      script:
        - cp ./tier1/ansible/ansible_vars.yaml.example ./tier1/ansible/ansible_vars.yaml
        - cp ./tier1/ansible/user_vars.yaml.example ./tier1/ansible/user_vars.yaml
        - ansible-lint --version
        - ansible-lint -x yaml ./tier1/ansible{main.yaml, manage_users.yaml}

    - name: Lint JSON
      language: node_js
      node_js: node
      before_script: npm install -g jsonlint
      script: find . -name "*.json" | xargs -n 1 jsonlint --quiet --compact

    - name: Lint Markdown
      language: ruby
      install: gem install mdl
      script: mdl --style .mdlstyle.rb *.md docs/

    - name: Lint PowerShell
      before_script:
        - sudo snap install powershell --classic
        - pwsh -c "Install-Module -Name PSScriptAnalyzer -Force"
      script: pwsh -c "Import-Module PSScriptAnalyzer; Invoke-ScriptAnalyzer -Path . -Settings .PSScriptAnalyzerSettings.psd1 -Recurse -EnableExit -ReportSummary"

    - name: Lint Python
      language: python
      python: 3.9
      before_script: pip install flake8
      script: flake8 . --statistics --count

    - name: Lint shell
      before_script: sudo apt-get install -y shellcheck
      script: find . -name "*.sh" | xargs shellcheck --format gcc --severity error

    - name: Lint YAML
      language: python
      python: 3.9
      before_script: pip install yamllint
      script: yamllint .

    - name: Test Powershell
      before_script:
        - sudo snap install powershell --classic
        - pwsh -c "Install-Module -Name Pester -Force; Install-Module -Name PSScriptAnalyzer -Force"
      script: pwsh -c './tests/Run_Pester_Tests.ps1'

    - name: Check for dead links in Markdown
      language: node_js
      node_js: node
      before_script: npm install -g markdown-link-check
      script: find . -name "*.md" | xargs -n 1 markdown-link-check -p -c .markdownlinkcheck.json
      env: ALLOWED_FAILURE=true

  allow_failures:
    - env: ALLOWED_FAILURE=true
